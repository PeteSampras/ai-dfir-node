# Copy to /etc/ainode/hunt.env. Consumed by ainode-hunt@.service.
# Window handed to the model for scheduled runs. Keep it comfortably larger than the
# timer interval so a delayed or skipped run does not leave an unexamined gap.
HUNT_WINDOW=2h
