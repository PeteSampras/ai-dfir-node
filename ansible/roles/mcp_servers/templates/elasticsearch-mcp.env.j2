ES_URL={{ elasticsearch_url }}
ES_API_KEY={{ elasticsearch_api_key }}
# MCP_TRANSPORT/MCP_PORT removed 2026-08-21: not real settings for this package -- transport
# is a CLI argument (see the Containerfile's own CMD ["http"]), not an env var. Leaving them
# here was a silent no-op that masked the real cause of a startup crash-loop.
